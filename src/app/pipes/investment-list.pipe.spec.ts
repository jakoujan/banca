import { InvestmentListPipe } from './investment-list.pipe';

describe('InvestmentListPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentListPipe();
    expect(pipe).toBeTruthy();
  });
});
